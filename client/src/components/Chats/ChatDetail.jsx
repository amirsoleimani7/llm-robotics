export default function ChatDetail() {
  return (
    <>
      <div className="max-w-[80%] h-auto self-end p-4 bg-second-color rounded-[2rem] break-words">
        testadasdasd dasdasd asdasd asdasd, adsdasd
      </div>

      <div className="w-full h-auto break-words">
        daasdasdasdWhy this happens: CSS normally only breaks at spaces,
        hyphens, or other break characters Long continuous strings (like
        dddddddd...) act as one word Without break rules, they extend beyond the
        container's max-width Best practice for chat/message bubbles: Use
        break-words (Tailwind) or word-break: break-word (CSS) to handle any
        unexpected long text without breaking your layout. Why this happens: CSS
        normally only breaks at spaces, hyphens, or other break characters Long
        continuous strings (like dddddddd...) act as one word Without break
        rules, they extend beyond the container's max-width Best practice for
        chat/message bubbles: Use break-words (Tailwind) or word-break:
        break-word (CSS) to handle any unexpected long text without breaking
        your layout. Why this happens: CSS normally only breaks at spaces,
        hyphens, or other break characters Long continuous strings (like
        dddddddd...) act as one word Without break rules, they extend beyond the
        container's max-width Best practice for chat/message bubbles: Use
        break-words (Tailwind) or word-break: break-word (CSS) to handle any
        unexpected long text without breaking your layout. Why this happens: CSS
        normally only breaks at spaces, hyphens, or other break characters Long
        continuous strings (like dddddddd...) act as one word Without break
        rules, they extend beyond the container's max-width Best practice for
        chat/message bubbles: Use break-words (Tailwind) or word-break:
        break-word (CSS) to handle any unexpected long text without breaking
        your layout. Why this happens: CSS normally only breaks at spaces,
        hyphens, or other break characters Long continuous strings (like
        dddddddd...) act as one word Without break rules, they extend beyond the
        container's max-width Best practice for chat/message bubbles: Use
        break-words (Tailwind) or word-break: break-word (CSS) to handle any
        unexpected long text without breaking your layout. Why this happens: CSS
        normally only breaks at spaces, hyphens, or other break characters Long
        continuous strings (like dddddddd...) act as one word Without break
        rules, they extend beyond the container's max-width Best practice for
        chat/message bubbles: Use break-words (Tailwind) or word-break:
        break-word (CSS) to handle any unexpected long text without breaking
        your layout. Why this happens: CSS normally only breaks at spaces,
        hyphens, or other break characters Long continuous strings (like
        dddddddd...) act as one word Without break rules, they extend beyond the
        container's max-width Best practice for chat/message bubbles: Use
        break-words (Tailwind) or word-break: break-word (CSS) to handle any
        unexpected long text without breaking your layout. Why this happens: CSS
        normally only breaks at spaces, hyphens, or other break characters Long
        continuous strings (like dddddddd...) act as one word Without break
        rules, they extend beyond the container's max-width Best practice for
        chat/message bubbles: Use break-words (Tailwind) or word-break:
        break-word (CSS) to handle any unexpected long text without breaking
        your layout. Why this happens: CSS normally only breaks at spaces,
        hyphens, or other break characters Long continuous strings (like
        dddddddd...) act as one word Without break rules, they extend beyond the
        container's max-width Best practice for chat/message bubbles: Use
        break-words (Tailwind) or word-break: break-word (CSS) to handle any
        unexpected long text without breaking your layout. Why this happens: CSS
        normally only breaks at spaces, hyphens, or other break characters Long
        continuous strings (like dddddddd...) act as one word Without break
        rules, they extend beyond the container's max-width Best practice for
        chat/message bubbles: Use break-words (Tailwind) or word-break:
        break-word (CSS) to handle any unexpected long text without breaking
        your layout. Why this happens: CSS normally only breaks at spaces,
        hyphens, or other break characters Long continuous strings (like
        dddddddd...) act as one word Without break rules, they extend beyond the
        container's max-width Best practice for chat/message bubbles: Use
        break-words (Tailwind) or word-break: break-word (CSS) to handle any
        unexpected long text without breaking your layout. Why this happens: CSS
        normally only breaks at spaces, hyphens, or other break characters Long
        continuous strings (like dddddddd...) act as one word Without break
        rules, they extend beyond the container's max-width Best practice for
        chat/message bubbles: Use break-words (Tailwind) or word-break:
        break-word (CSS) to handle any unexpected long text without breaking
        your layout. Why this happens: CSS normally only breaks at spaces,
        hyphens, or other break characters Long continuous strings (like
        dddddddd...) act as one word Without break rules, they extend beyond the
        container's max-width Best practice for chat/message bubbles: Use
        break-words (Tailwind) or word-break: break-word (CSS) to handle any
        unexpected long text without breaking your layout. Why this happens: CSS
        normally only breaks at spaces, hyphens, or other break characters Long
        continuous strings (like dddddddd...) act as one word Without break
        rules, they extend beyond the container's max-width Best practice for
        chat/message bubbles: Use break-words (Tailwind) or word-break:
        break-word (CSS) to handle any unexpected long text without breaking
        your layout. Why this happens: CSS normally only breaks at spaces,
        hyphens, or other break characters Long continuous strings (like
        dddddddd...) act as one word Without break rules, they extend beyond the
        container's max-width Best practice for chat/message bubbles: Use
        break-words (Tailwind) or word-break: break-word (CSS) to handle any
        unexpected long text without breaking your layout. Why this happens: CSS
        normally only breaks at spaces, hyphens, or other break characters Long
        continuous strings (like dddddddd...) act as one word Without break
        rules, they extend beyond the container's max-width Best practice for
        chat/message bubbles: Use break-words (Tailwind) or word-break:
        break-word (CSS) to handle any unexpected long text without breaking
        your layout. Why this happens: CSS normally only breaks at spaces,
        hyphens, or other break characters Long continuous strings (like
        dddddddd...) act as one word Without break rules, they extend beyond the
        container's max-width Best practice for chat/message bubbles: Use
        break-words (Tailwind) or word-break: break-word (CSS) to handle any
        unexpected long text without breaking your layout. Why this happens: CSS
        normally only breaks at spaces, hyphens, or other break characters Long
        continuous strings (like dddddddd...) act as one word Without break
        rules, they extend beyond the container's max-width Best practice for
        chat/message bubbles: Use break-words (Tailwind) or word-break:
        break-word (CSS) to handle any unexpected long text without breaking
        your layout. Why this happens: CSS normally only breaks at spaces,
        hyphens, or other break characters Long continuous strings (like
        dddddddd...) act as one word Without break rules, they extend beyond the
        container's max-width Best practice for chat/message bubbles: Use
        break-words (Tailwind) or word-break: break-word (CSS) to handle any
        unexpected long text without breaking your layout. Why this happens: CSS
        normally only breaks at spaces, hyphens, or other break characters Long
        continuous strings (like dddddddd...) act as one word Without break
        rules, they extend beyond the container's max-width Best practice for
        chat/message bubbles: Use break-words (Tailwind) or word-break:
        break-word (CSS) to handle any unexpected long text without breaking
        your layout. Why this happens: CSS normally only breaks at spaces,
        hyphens, or other break characters Long continuous strings (like
        dddddddd...) act as one word Without break rules, they extend beyond the
        container's max-width Best practice for chat/message bubbles: Use
        break-words (Tailwind) or word-break: break-word (CSS) to handle any
        unexpected long text without breaking your layout. Why this happens: CSS
        normally only breaks at spaces, hyphens, or other break characters Long
        continuous strings (like dddddddd...) act as one word Without break
        rules, they extend beyond the container's max-width Best practice for
        chat/message bubbles: Use break-words (Tailwind) or word-break:
        break-word (CSS) to handle any unexpected long text without breaking
        your layout. Why this happens: CSS normally only breaks at spaces,
        hyphens, or other break characters Long continuous strings (like
        dddddddd...) act as one word Without break rules, they extend beyond the
        container's max-width Best practice for chat/message bubbles: Use
        break-words (Tailwind) or word-break: break-word (CSS) to handle any
        unexpected long text without breaking your layout. Why this happens: CSS
        normally only breaks at spaces, hyphens, or other break characters Long
        continuous strings (like dddddddd...) act as one word Without break
        rules, they extend beyond the container's max-width Best practice for
        chat/message bubbles: Use break-words (Tailwind) or word-break:
        break-word (CSS) to handle any unexpected long text without breaking
        your layout. Why this happens: CSS normally only breaks at spaces,
        hyphens, or other break characters Long continuous strings (like
        dddddddd...) act as one word Without break rules, they extend beyond the
        container's max-width Best practice for chat/message bubbles: Use
        break-words (Tailwind) or word-break: break-word (CSS) to handle any
        unexpected long text without breaking your layout.
      </div>
    </>
  );
}
